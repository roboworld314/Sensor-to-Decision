---
// Hero.astro — Final Centered Interactive Version
---

<section id="hero" class="hero-section">

  <div class="hero-inner">

    <!-- LEFT CONTENT -->
    <div class="hero-left">
      <h1 class="hero-title">
        SenseGuard: Sensor-to-Decision Adaptation for Reliable Agentic AI Robots
      </h1>

      <p class="hero-subtitle">
        Real-Time Adaptive Perception Decision Making under Vibration and Resource Constraints
      </p>

      <!-- Paper Box -->
      <div class="paper-box">
        <div class="paper-left">
          <span class="paper-icon">📄</span>
          <span class="paper-title">Supplementary Materials</span>
        </div>

        <div class="paper-links">

          <!-- Appendix -->
          <a href="https://drive.google.com/file/d/1adc8mrF3CoUz2dJ_2IxYwd5p2cFx412H/view"
             target="_blank" class="paper-btn paper-btn-primary">
            Appendix
          </a>

          <!-- Code -->
          <a href="#" class="paper-btn paper-btn-secondary"
             onclick="alert('We will publish soon!'); return false;">
            Code
          </a>

          <!-- Dataset -->
          <a href="#" class="paper-btn paper-btn-secondary"
             onclick="alert('We will publish soon!'); return false;">
            Dataset
          </a>

          <!-- Video -->
          <a href="/video" class="paper-btn paper-btn-primary">
            Demo Video
          </a>

        </div>
      </div>
    </div>

    <!-- RIGHT VISUAL -->
    <div class="hero-right">
      <div class="vehicle-wrapper">
        <img 
          src="https://cdn-icons-png.flaticon.com/512/744/744465.png" 
          alt="Autonomous Vehicle"
          class="vehicle"
        />
      </div>
    </div>

  </div>
</section>

<style>

/* ===== BASE ===== */
.hero-section {
  padding: 4rem 2rem;
  background: linear-gradient(135deg, #f7f5f2, #ece9e3);
}

/* ===== GRID LAYOUT ===== */
.hero-inner {
  max-width: 1100px;
  margin: auto;

  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  justify-items: center;
  gap: 2rem;
}

/* ===== LEFT (CENTERED CONTENT) ===== */
.hero-left {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* ===== TITLE ===== */
.hero-title {
  font-size: 2.8rem;
  line-height: 1.2;
  text-align: center;

  background: linear-gradient(135deg, #1a2740, #2f4a85);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* ===== SUBTITLE ===== */
.hero-subtitle {
  margin-top: 0.7rem;
  font-size: 1.05rem;
  color: #5c667a;
  text-align: center;
}

/* ===== VEHICLE ===== */
.hero-right {
  display: flex;
  justify-content: center;
  align-items: center;
}

.vehicle-wrapper {
  width: 260px;
}

/* floating animation */
.vehicle {
  width: 100%;
  opacity: 0.9;

  animation: floatCar 4s ease-in-out infinite;
  transition: transform 0.3s ease;
}

.vehicle:hover {
  transform: scale(1.06);
}

@keyframes floatCar {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
  100% { transform: translateY(0px); }
}

/* ===== PAPER BOX ===== */
.paper-box {
  margin-top: 2rem;
  padding: 1rem;

  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.6rem;

  border-radius: 12px;
  background: rgba(255,255,255,0.75);
  backdrop-filter: blur(10px);

  border: 1px solid rgba(0,0,0,0.05);
  transition: 0.25s ease;
}

.paper-box:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 25px rgba(0,0,0,0.08);
}

/* ===== LEFT LABEL ===== */
.paper-left {
  font-weight: 600;
  color: #223253;
  font-size: 0.9rem;
}

/* ===== BUTTONS ===== */
.paper-links {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
  justify-content: center;
}

.paper-btn {
  padding: 0.45rem 0.9rem;
  border-radius: 8px;
  font-size: 0.8rem;
  font-weight: 500;
  text-decoration: none;
  transition: all 0.25s ease;
}

/* Primary */
.paper-btn-primary {
  background: linear-gradient(135deg, #1f2f54, #2f4a85);
  color: white;
  box-shadow: 0 3px 12px rgba(47, 74, 133, 0.25);
}

.paper-btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(47, 74, 133, 0.35);
}

/* Secondary */
.paper-btn-secondary {
  border: 1px solid #d6dbe4;
  color: #3a445a;
  background: transparent;
}

.paper-btn-secondary:hover {
  background: #223253;
  color: white;
}

/* ===== RESPONSIVE ===== */
@media (max-width: 768px) {
  .hero-inner {
    grid-template-columns: 1fr;
    text-align: center;
  }

  .hero-title {
    font-size: 2rem;
  }

  .vehicle-wrapper {
    width: 180px;
  }
}

</style>